# Character Architect - Environment Configuration
# Copy to .env.local and customize for your setup

# =============================================================================
# DEPLOYMENT SCENARIOS
# =============================================================================

# --- LOCAL DEV (localhost only) ---
# Default settings work out of the box. No changes needed.

# --- LAN DEV (linux host, test from other devices) ---
# Uncomment and configure:
# HOST=0.0.0.0
# VITE_ALLOWED_HOSTS=192.168.1.100,myserver.local
# CORS_ORIGINS=http://localhost:5173,http://192.168.1.100:5173

# --- CLOUDFLARE PAGES (static PWA) ---
# Set in Cloudflare Pages environment variables:
# VITE_DEPLOYMENT_MODE=static

# --- DOCKER (production) ---
# Edit docker-compose.yml for LAN access:
# Change "127.0.0.1:8765:80" to "0.0.0.0:8765:80"

# =============================================================================
# API CONFIGURATION (apps/api)
# =============================================================================

# Network binding (127.0.0.1 = localhost only, 0.0.0.0 = all interfaces)
# HOST=127.0.0.1
# PORT=3456

# CORS origins (comma-separated, or '*' for all - DEV ONLY)
# CORS_ORIGINS=http://localhost:5173

# Rate limiting
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_MAX=1000

# =============================================================================
# WEB CONFIGURATION (apps/web)
# =============================================================================

# Deployment mode: full | light | static
# Auto-detected from hostname if not set
# VITE_DEPLOYMENT_MODE=full

# Allowed hosts for dev server (comma-separated)
# VITE_ALLOWED_HOSTS=192.168.1.100,myserver.local
