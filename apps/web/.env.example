# Web Frontend Configuration

# Deployment Mode
# - 'full': All features, local/self-hosted with API (default for localhost/LAN)
# - 'light': IndexedDB only, no server features (auto-detected for remote hosts)
# - 'static': Pure client-side, Cloudflare Pages / GitHub Pages
# Auto-detection: localhost/LAN IPs -> full, remote hosts -> light
# VITE_DEPLOYMENT_MODE=full

# LAN Development
# When running on a LAN server (e.g., 192.168.x.x), add allowed hosts:
# VITE_ALLOWED_HOSTS=192.168.1.100,myserver.local

# API URL (for when API is on a different host/port)
# VITE_API_URL=http://localhost:3456
